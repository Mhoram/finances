/**
 * CGT API configuration. Copy this file to cgt-config.js (gitignored, like
 * config.js) and adjust for your deployment. If cgt-config.js is missing,
 * CGT_API_BASE defaults to a same-origin relative path (see cgt-ui.js) —
 * that works if you reverse-proxy /api/* to the server, otherwise set the
 * full URL here.
 */
const CGT_API_BASE = 'http://localhost:3001/api/v1';
